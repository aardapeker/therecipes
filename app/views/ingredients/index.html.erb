<%= render 'shared/page_title', title: "available ingredients" %>

<div align="center">
  <%= will_paginate %>
  <% @ingredients.each do |ing|  %>
    <ul class="listing">
      <div class="row fix-container">
        <div class="well col-md-4 col-md-offset-4 asdasd">

          <li class="chef-title"><%= link_to ing.name.capitalize, ingredient_path(ing) %></li>
          <li><small><%= pluralize(ing.recipes.count, "recipe") if ing.recipes.present? %></small></li>
          <% if logged_in? && current_chef.admin? %>
          <li><%= link_to "Edit this ingredient", edit_ingredient_path(ing) %></li>
          <% end %>
        </div>
      </div>
    </ul>
  <% end %>
  <%= will_paginate %>
</div>
